# Secrets
.env
*.env
!.env.example

# Financiële / geëxporteerde documenten horen niet in GitHub (CM governance).
# Bewaar deze in Google Drive en geef het pad mee via --invoices.
*.zip
*.xlsx
*.xls
*.csv

# Uitvoer (gegenereerde reports + lokale SQLite-cache)
output/
CM_FINANCE_RECOVERY/output/
reports/
CM_FINANCE_RECOVERY/reports/
*.db
CM_FINANCE_RECOVERY/data/*.db

# Let op: data/moneybird_sync.json is bewust WEL getrackt (input voor de pipeline).

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Door Moneybird herkende (OCR) leverancier/bedrag = gevoelig -> niet in Git.
# (De .sample.json met dummy-data is bewust WEL getrackt als format-voorbeeld.)
CM_FINANCE_RECOVERY/data/moneybird_recognition.json
